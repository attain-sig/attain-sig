<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RM_is_Current_User__c</fullName>
    <description>Formula field that looks at the running user's User Contact ID (field on the user record that ties the user to their contact record) and compares it to the Contact ID from the Relationship Manager lookup. Used to return the running users' accounts where they are the RM</description>
    <externalId>false</externalId>
    <formula>$User.User_Contact_ID__c =  Account_Primary_Handler__c|| 
$User.User_Contact_ID__c =   Additional_Handler_1__c|| 
$User.User_Contact_ID__c =   Additional_Handler_2__c</formula>
    <label>Account RM is Current User</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
